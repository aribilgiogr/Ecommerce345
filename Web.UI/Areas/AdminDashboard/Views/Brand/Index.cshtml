@model IEnumerable<BrandListDto>
@{
    ViewData["Title"] = "Markalar";
}
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Marka Yönetimi</h1>
    <a asp-action="create" asp-controller="brand" class="btn btn-primary shadow-sm">
        <i class="bi bi-plus-lg me-1"></i> Yeni Marka Ekle
    </a>
</div>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-white py-3">
        <h6 class="m-0 fw-bold text-primary">Marka Listesi</h6>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle m-0">
                <thead class="table-light">
                    <tr>
                        <th>Logo</th>
                        <th>Marka Adı</th>
                        <th class="text-end">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var brand in Model)
                        {
                            <tr>
                                <td>
                                    @if (brand.LogoUrl != null)
                                    {
                                        <img src="@brand.LogoUrl" alt="@brand.Name" class="img-thumbnail" />
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">Logo Yok</span>
                                    }
                                </td>
                                <td>
                                    <span class="fw-medium">@brand.Name</span>
                                </td>
                                <td class="text-end">
                                    <div class="btn-group" role="group">
                                        <a asp-action="edit" asp-controller="brand" asp-route-id="@brand.Id" class="btn btn-outline-primary btn-sm">Düzenle</a>
                                        <form method="post" asp-action="delete" asp-controller="brand" asp-route-id="@brand.Id" class="d-inline" onsubmit="return confirm('Bu markayı silmek istediğinize emin misiniz?')">
                                            <button type="submit" class="btn btn-outline-danger btn-sm">Sil</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="3" class="text-center py-4 text-muted">
                                Henüz hiç marka eklenmemiş!
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
